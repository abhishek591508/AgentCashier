{
  "name": "agentcashier",
  "version": "1.0.0",
  "private": true,
  "description": "Gated agentic commerce: AI buyer shops, cashier pays via Razorpay test mode.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "eval": "npm run eval --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
